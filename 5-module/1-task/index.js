'use strict';

/**
 * Метод устанавливает необходимые по условию аттрибуты таблице
 * @param {Element} table
 */
function highlight(table) {
    let teachersTable = document.getElementsByClassName("js-teachers");
    let td = teachersTable.getElementsByTagName("td");
    alert(td);
}